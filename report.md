# 最终项目报告

> 本报告记录可复查证据，不是宣传文案。所有数字来自当前代码实际输出。

## 摘要

我们为回收站分类员制作垃圾图像分类工具：输入一张垃圾照片，输出可回收物/厨余垃圾/有害垃圾/其他垃圾四类之一，帮助第一级分拣。
使用真实数据：Kaggle Garbage classification dataset，Apache 2.0，55 细类合并成 4 类、共 12094 张，固定 80/20 划分。
基线为多数类，准确率 0.4261；候选一为小 CNN，准确率 0.6268；候选二为迁移学习（预训练 ResNet18 提取特征 + 线性分类器），准确率 0.8858，比基线高 +0.4597。迁移学习大幅改善每类准确率，尤其可回收物从 0.39 提升到 0.80。
边界：只在四类间判断，低置信度人工复核，不做自动处罚等高风险决定。

## 1. 问题与范围

- 项目句子：我们为回收站分类员制作垃圾识别与分类工具，它接收一张垃圾照片，输出"可回收/厨余/有害/其他"四类之一，帮助完成第一级分拣；尤其不能忽略把有害垃圾误判成其他类。
- 使用者与决定：我们为回收站分类员制作垃圾识别与分类工具，帮助分类垃圾。
- 输入与输出：输入=一张真实垃圾照片；输出=四类之一（可回收物/厨余垃圾/有害垃圾/其他垃圾）
- 普通案例：输入：一张真实的毛巾照片img_毛巾_185.jpeg
  期望输出：其他垃圾，模型输出正确
  为何有用：毛巾是常见其他垃圾，系统能认出来→分拣员不用仔细看就能放进对的桶，省时间
- 困难/拒绝案例：输入：一张胶带照片
  期望输出或拒绝：理想应输出其他垃圾；如果模型低置信度，应该提示分拣员人工复核，而不是直接按结果扔
  主要风险：可回收物如塑料瓶可能被误判成其他垃圾，导致可回收资源被当普通垃圾处理、流失
- 核心范围：只用真实图片数据做 4 类分类；不做 55 细类识别、不做检测/定位、不部署
- 明确不做：不做实时视频、不做自动罚款/决策、不做医疗/安全等高风险自动判断

## 2. 真实数据契约

- 所有者/发布者：shan lan（Kaggle 用户 shanlan426）
- 数据集标题与版本：Garbage classification dataset
- 原始 URL：https://www.kaggle.com/datasets/shanlan426/garbage-classification-dataset
- 许可、同意或使用权限：Apache 2.0
- 下载日期与步骤：2026-08-26，Kaggle 页面 Download → 解压 → 放入 data/raw/dataset/
- 预期文件、字段、数量范围：data/raw/dataset/ 下 55 个细类文件夹（命名 `大类_细类`）；按前缀合并成 4 大类：厨余垃圾 5153 / 可回收物 3331 / 有害垃圾 2158 / 其他垃圾 1452（共 12094 张）；数据无内置划分，用 make_split.py 固定 80/20 划分
- 数据检查命令与结果：`python make_split.py` → 生成 split.json，打印各 4 大类训练/测试数量
- Git 排除：data/raw/（.gitignore 已排除，大图不提交）
- 敏感字段：无个人信息
- 缺失、偏差、代表性与时间限制：缺失/偏差：数据是室内白底拍摄，缺少户外、脏乱、光照差等场景；类别不均衡，厨余：5153最多、其他：1452最少，少数类表现更差。代表性：这是某时点下载的快照，垃圾外观会随季节或地区变化。
- 允许/不允许的结论：允许的结论：在这个数据集、固定划分下，候选比基线有用，可回收物最不准。不允许的结论：不能推广到户外脏乱等所有真实场景；不能用于自动罚款等高风险决策。

## 3. 系统设计

真实输入→make_split 划分→缩到64×64→基线(多数类)/候选(CNN)→评估→输出

## 4. 可复现方法

- 环境和依赖：Python + torch 2.13.0 + Pillow
- 安装命令：`pip install torch pillow pytest`（torch CPU 版：`pip install torch --index-url https://download.pytorch.org/whl/cpu`）
- 数据放置：data/raw/dataset/（README 写下载方法，不提交大文件）
- 运行命令：
  - 划分：`python make_split.py`
  - 基线：`python baseline4.py`
  - 候选：`python train_candidate4.py`
- 测试命令：`python -m pytest tests/`（检查 split.json 数据契约，当前 2 passed）
- 输出文件：split.json、baseline_result.txt、candidate_result.txt、failure_examples.txt、success_examples.txt
- 随机种子、时间范围或固定条件：SEED=42；80/20 固定划分（make_split.py）；TRAIN_LIMIT=4000；IMG_SIZE=64；EPOCHS=6

## 5. 基线与候选

基线为什么选多数类：最简单、不看图片也能解释，是最低标准。
候选一：小 CNN，从零训练，比基线复杂一步。
候选二：迁移学习——用 ImageNet 预训练的 ResNet18 提取特征（冻结、不修改），只训练最后一层 512→4 的线性分类器。
怎么同条件：基线和两个候选都用同一个 split.json、同一个测试集 2417 张、同一个准确率指标比较。

## 6. 评估设计与结果

- 划分/评估协议：make_split.py 固定 80/20（SEED=42）；训练子集 4000 张（每类 1000）；评估用完整 TEST 2417 张，与基线同条件
- 主指标与使用者含义：总准确率=猜对÷全部；同时必须看每类准确率（总准确率会被多数类"厨余"拉高）
- 辅助错误计数：失败案例数 902；每类准确率见下表
- 测试集如何保持独立：模型训练时没看过 TEST 任何一张
一（小CNN） | 候选二（迁移学习） | 条件与解释 |
| --- | ---: | ---: | ---: | --- |
| 主指标（准确率） | 0.4261 (1030/2417) | 0.6268 (1515/2417) | 0.8858 (2141/2417) | 同一 TEST 2417 张（固定 80/20 划分） |
| 重要错误（失败数） | — | 902 | 276 | 候选二失败数远少于候选一 |
| 随机瞎猜 | 0.2500 | — | — | 4 类随机概率，多数类基线已高于随机 |
| 实验：EPOCHS=3 | — | 0.5602 | — | 训练遍数少 → 准确率低 |
| 实验：TRAIN_LIMIT=2000 | — | 0.6181 | — → 准确率低 |
| 实验：TRAIN_LIMIT=2000 | — | 0.6181 | 训练数据少 → 准确率低；选定 TRAIN_LIMIT=4000 |

每类准确率（测试集 2417 张）：
| 类别 | 候选一（小CNN） | 候选二（迁移学习） |
| --- | ---: | ---: |
| 厨余垃圾 | 0.8515 | 0.9583 |
| 其他垃圾 | 0.5724 | 0.8138 |
| 有害垃圾 | 0.4919 | 0.8910 |
| 可回收物 | 0.3904 | 0.8018 |

> 关键发现：候选一（小 CNN）总准确率 0.6268 被多数类"厨余"拉高，每类视角下可回收物最难（0.3904）。候选二（迁移学习）大幅改善所有类别（0.8858），可回收物提升到 0.80——说明小数据下借用预训练特征比从零训练更有效。对使用者影响最大的仍是可回收物被误判（可回收资源流失），即便候选二里它仍是相对较弱的一类。

## 7. 成功与失败案例

- 成功案例：测试集真实图 `img_毛巾_185.jpeg`，真值=其他垃圾，模型预测正确（其他垃圾）。观察：毛巾有清晰的布料纹理，外观是柔软织物，和厨余、金属塑料等差异明显，所以模型能分对。这证明模型能正确识别视觉特征明显的"其他垃圾"；不能证明所有"其他垃圾"都能分对——从每类准确率看"可回收物"仍然最难。
- 失败案例（最终模型：候选二 迁移学习）：
  - 具体案例：测试集真实图 `img_湿纸巾_72.jpeg`，真值=其他垃圾，预测=可回收物。观察：湿纸巾的外层塑料包装看起来像可回收塑料制品，被误判。
  - 类级发现：候选二每类准确率 0.80–0.96；可回收物仍相对较弱（0.80）。注意：不能只看"前几个失败"——它按测试顺序取前 10 个会误导，要按类统计。
  - 对使用者的影响：可回收物被误判 = 可回收资源被当普通垃圾处理、流失。
  - 证明/不证明：证明即便迁移学习（0.8858）仍会错（失败 276/2417）；不能证明模型无用，也不能证明误判的确切视觉机制。
  - 过程：候选一（小 CNN）里可回收物最弱（0.39，204/666 误判成其他垃圾）；迁移学习把它提升到 0.80——这是我们用每类准确率/混淆矩阵分析后得到的重要发现。

## 8. 安全、伦理与人工边界

无密钥、无个人信息，只用获批的公开数据（Apache 2.0）
只在四类间判断；低置信度时提示人工复核，不直接按结果扔
不做自动罚款、医疗、安全等高风险自动决策

## 9. 智能体贡献与学生所有权

智能体给了示例脚本和步骤、帮助排查错误
学生所有权：下载并检查数据、跑每个实验、改 EPOCHS 和 TRAIN_LIMIT、写成功/失败案例、质疑"其他垃圾最难"的判断并加混淆矩阵验证
每名成员能独立解释自己负责的部分

## 10. 限制与下一步

数据：白底室内拍摄、类别不均衡：厨余最多，其他最少；
方法：64×64小图+小模型，可回收物类最弱，准确率为0.39
评估：总准确率被"厨余"拉高，必须看每类
产品：只适用白底照片，不能自动决策
尝试：曾尝试迁移学习（ImageNet 预训练 ResNet18 提取特征 + 线性分类器），因本机网络无法下载约 45MB 预训练权重而未完成；正式候选保留小 CNN。
下一步：给可回收物类加数据，或把图提到96×96，在同一个划分下重新比较，看是否超过0.6268

## 11. 最终复核

- [ ] 新环境可按 README 运行
- [ ] 数据契约与检查完整
- [ ] 基线和候选同条件
- [ ] 测试/评估与保存输出一致
- [ ] 失败案例和边界具体
- [ ] 10 分钟 PPT 计时通过
- [ ] 每名成员能回答教师追问
- [ ] 无密钥、大数据、个人信息、环境或缓存
- [ ] GitHub 网页和访问权限复查
